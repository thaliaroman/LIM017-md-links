{
  "name": "md-links",
  "version": "0.1.0",
  "engines": {
    "node": ">=14.x"
  },
  "dependencies": {
    "jsdom": "^19.0.0",
    "markdown-it": "^13.0.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "jest": "^28.1.0"
  },
  "scripts": {
    "test": "jest --verbose --coverage"
  }
}
